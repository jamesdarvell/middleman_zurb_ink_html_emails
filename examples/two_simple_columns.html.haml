---
title: Example with two columns in one row
---
= ink_boilerplate do
  = ink_container(:classes => %w{main area}) do
    = ink_row do
      =ink_column('eight') do
        %td
          Eight Columns

      =ink_column('four') do
        %td
          Four Columns